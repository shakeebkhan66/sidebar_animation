import 'package:flutter/material.dart';

class checkRequest extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("CHECK REQUESTS", style: TextStyle(fontWeight: FontWeight.bold, fontSize: 20),),
      ),
    );
  }
}